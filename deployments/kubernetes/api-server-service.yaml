apiVersion: v1
kind: Service
metadata:
  name: sdkwork-agents-api-server
  labels:
    app.kubernetes.io/name: sdkwork-agents
    app.kubernetes.io/component: api-server
spec:
  selector:
    app.kubernetes.io/name: sdkwork-agents
    app.kubernetes.io/component: api-server
  ports:
    - name: http
      port: 8095
      targetPort: http
  type: ClusterIP
