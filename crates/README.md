# Rust Crates

| Crate | Responsibility |
| --- | --- |
| `sdkwork-agents-contract` | Runtime env helpers (`sdkwork-utils-rust`) |
| `sdkwork-agents-kernel-bridge` | Composition boundary over `sdkwork-kernel` |
| `sdkwork-agents-api-server` | Runnable HTTP server process |

Kernel-owned route crates remain in `../sdkwork-kernel/crates/sdkwork-routes-agent-*`.
