# zh-CN locale seeds

Add locale-specific seed SQL referenced from `seeds/seed.manifest.json`.

# zh-CN seed set

No locale-dependent default content exists for Agents: all business rows
(agents, workspaces, sessions, tasks) are created by tenants at runtime.
This set is intentionally empty; the manifest records the empty-content
checksum so seed validation stays deterministic.
