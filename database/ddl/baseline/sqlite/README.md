# SQLite baseline (retired)

SDKWork Agents persistence is **PostgreSQL-only** for managed-store production paths.

Use `database/ddl/baseline/postgres/0001_agents_baseline.sql` as the single DDL authority.
