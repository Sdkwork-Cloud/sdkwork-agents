# SDKWork Agent SDK Specs

This directory declares the component manifest for the developer/open SDK
family `sdkwork-agents-sdk`.

The OpenAPI authority is `sdkwork-agents-open-api`, and generated transport
output is owned by `sdkwork-agents-sdk-typescript/generated/server-openapi`.
