export interface ToolAssetView {
  toolId: string;
  toolCallId: string;
  mediaKind: string;
  driveSpaceId: string;
  driveNodeId: string;
  driveUri: string;
  sourceUrl?: string;
  createdAt?: string;
}
