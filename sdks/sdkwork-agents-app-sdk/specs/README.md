# SDKWork Agent App SDK Specs

This directory declares the component manifest for the app SDK family
`sdkwork-agents-app-sdk`.

The OpenAPI authority is `sdkwork-agents-app-api`, and generated transport
output is owned by
`sdkwork-agents-app-sdk-typescript/generated/server-openapi`.
