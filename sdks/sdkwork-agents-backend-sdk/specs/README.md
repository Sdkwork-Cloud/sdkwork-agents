# SDKWork Agent Backend SDK Specs

This directory declares the component manifest for the backend SDK family
`sdkwork-agents-backend-sdk`.

The OpenAPI authority is `sdkwork-agents-backend-api`, and generated transport
output is owned by
`sdkwork-agents-backend-sdk-typescript/generated/server-openapi`.
