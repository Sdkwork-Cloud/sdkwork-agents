# SDKWork Agents PC Specifications

The machine-readable application contract is `component.spec.json`. Canonical
frontend, PC architecture, IAM login, SDK integration, code style, naming, and
test rules remain owned by `../../../sdkwork-specs/`.
